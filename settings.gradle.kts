pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Mobile Passive Data SDK"
include(":androidApp")
include(":passiveData")