package org.sagebionetworks.assessmentmodel.passivedata.internal

import org.sagebionetworks.assessmentmodel.StringEnum as AssessmentModelStringEnum

/**
 * A string enum is an enum that uses a string as its raw value.
 */
actual interface StringEnum : AssessmentModelStringEnum